export const sort = (array) => (array.sort(()=> (Math.random() - 0.5)))

export const randomItem = (array) => (array[Math.floor(Math.random() * array.length)])

export default (w)=>{
    return `${w}ssss`
}